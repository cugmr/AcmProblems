/*#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

dp[n][s][x]:长度为n，和为s，最后一个为x的种类
dp[n][s][x] = dp[n][s][x]

ll n, s, dp[105][505][505];
ll dfs(ll now, ll sum, ll last)
{
	if (dp[now][sum][last] != -1) return dp[now][sum][last];
	if (now == n) return dp[now][sum][last] = 1;
	dfs(now + 1, sum + a
}
int main()
{

	while (cin >> n >> s >> a >> b)
	{

	}
}*/