/*#include <bits/stdc++.h>
using namespace std;
int a[110];
int main(){
	int x, n;
	while (cin >> n)
	{
		memset(a, 0, sizeof(a));
		while (n--)
		{
			cin >> x;
			a[x]++;
		}
		sort(a, a + 105, greater<int>());
		cout << a[0] << endl;
	}
}
*/